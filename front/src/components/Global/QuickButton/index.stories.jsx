import React from 'react';
import QuickButton from 'components/Global/QuickButton';

export default {
  title: 'components/QuickButton',
  component: QuickButton,
};

const Template = (args) => <QuickButton {...args} />;

export const Default = Template.bind({});
